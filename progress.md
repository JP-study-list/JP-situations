# progress.md — 開發歷史

> 反向時間序（最新在上）。每次改檔即更新。
> 小改可精簡為「日期 + 類型 + 摘要」。

---

## 2026-08-07（三）
- 類型：新增
- 影響檔案：`bank-account.html`（新增）, `index.html`, `audio/`（新增 245 個 mp3）,
  `project-index.md`
- 摘要：新增**銀行開戶**情境頁（`pageKey: bank-account`，主色千歳緑 `#316745`），
  主題為**留學生在 ゆうちょ銀行 開戶**。收錄 4 場景 x 4 情境 x 12-14 句 = 210 句、
  48 個單字、6 組易混詞、約 300 筆假名字典。
  於 `index.html`「日本在住者」區塊補卡片（排在入境審查與郵局之間）、
  `PAGES` 項目與檔尾 `SCENE`（全站情境頁 27 -> 28）。
- 原因：使用者即將赴日念語言學校並辦資格外活動許可，開戶是落地後的第一批手續，
  且最容易因為「資料沒帶齊」而白跑一趟。使用者要求**把需要準備的資料融入情境之間**，
  不要另做一份條列清單。
- **與 `post-office.html` 的分工（重要，日後勿誤合併）**：
  規劃時查到 `post-office.html` 的「郵局金融服務」場景**已有一個 13 句的「開設帳戶」情境**，
  且 VOCAB 有一整類「郵局金融（ゆうちょ）」14 個字。判斷不必因此放棄 ゆうちょ 設定
  （留學生實務上多半只開得成 ゆうちょ），改為明確分工：
  - `post-office.html` 保留淺層流程，一字未改
  - `bank-account.html` 走**資料準備與卡關應對**，16 個情境 210 句
  - 新頁 VOCAB **刻意避開**那 14 個字。唯一相近的是「本人確認」對上「本人確認書類」
    （動作 vs 文件，句中用法不同，刻意各留一個）
- 內容設計：
  - 四場景為完整動線：事前確認（能否開戶／必要書類／學生身分證明／抽號碼牌）->
    申込與確認（填申込書／在留卡與個人編號／姓名表記與住址／利用目的）->
    貯金與卡片（貯金種類／記号與番号／提款卡與網銀／首次存入）->
    資料不齊（在留卡背面未記載／個人編號未取得／沒有日本手機號碼／備齊再來）。
  - 依使用者選擇，前三場景走順利流程、第四場景練卡關（比照 `immigration.html`）。
  - **「資格外活動許可」的出場位置**是「利用目的」情境，由客人主動出示在留卡背面，
    而非行員盤問——因為職業填「學生」卻要選「給与受取」時，這張許可正是讓兩者說得通的東西。
    這是「需要準備的資料融入情境」的核心設計。
  - **`記号・番号` 獨立教**：ゆうちょ 用記号 5 碼＋番号 8 碼，跨行匯入要換算成店名與番号，
    收學費與打工薪資轉帳一定會遇到，且 `post-office.html` 沒教。
  - 個人背景用通用佔位（さくら日本語学校／新宿区／在学一年），方便日後替換成本人資料。
- **刻意保守處理的事實**：ゆうちょ 對在留期間未滿六個月者，實務上是「可能要追加資料」
  而非一律拒絕，且各分局做法不一。行員台詞一律寫成
  「在留期間によっては追加書類が必要になることがございます」，
  **不寫死成「開設できません」**，避免教到會過期或因地而異的規則。
- 驗證（全部自動化，48 + 20 + 27 項）：
  - 資料層 48 項：`node --check` 通過；4 分類各 12 字、6 組易混各 2-3 items、
    4 場景各 4 情境各 12-14 句（合計 210 句）；`pageKey` 全站 28 頁唯一；
    `defaultCat` / `defaultScene` 存在；10 個必要顏色變數齊全；
    無 emoji、無殘留引擎碼（`KANJI_READINGS.sort` / `buildFuriganaHTML` / 三個狀態宣告）；
    載入順序與 `?v=2` 版本號正確。
  - **KANJI_READINGS 兩道檢查**：抄 `app.js` 的最長匹配邏輯掃過 210 句，漢字**零缺漏**；
    再做**反向重建**（用字典把 `jp` 還原成假名與 `kana` 欄逐字比對），210 句全等。
    第一輪抓到 44 句不一致、39 個漏字，補了 30 個字典項並修掉
    「本人様」讀音誤含「ご」與一處 `kana` 誤植（`ききいて` -> `きいて`）。
  - `index.html` 20 項：卡片／`PAGES`／`SCENE` 三處各 28 筆且互相無孤兒、
    `card-title` 與 `PAGES.title` 完全一致、`SCENE` key 都對應實際檔案、
    四個區塊內相鄰卡片不同色、28 個場景色全站不重複。
  - **headless Chrome 實跑（CDP）27 項**：三個主分頁、四個單字分類與 12 張卡、
    四個常用句場景、情境一與資料不齊[情境四]各渲染 13 句、furigana 39 個標記、
    說話者標牌為「行員／客人」、每句都有發音鈕與收藏鈕、無橫向溢出、無未捕捉例外；
    首頁 28 張卡都取到 `--shop` 與圖示、分類順序與在住者區塊排序正確、
    銀行開戶卡片色為 `#316745`。
  - 深色模式：`--primary` 提亮為 `#7FB294`，暖簾頂欄對比度 **16.84**（門檻 4.5）。
  - 淺色與深色各截圖目視確認。
  - **語音端對端**：用 `app.js` 自身的 `audioHash` 與 `scenarioVoices` 算出本頁
    全部 273 個播放點（對話 210 / 單字 48 / 易混 15）的音檔 URL，逐一查存在，
    命中 **273/273 = 100%**，線上不會退回 Web Speech。
    16 組情境音色配對正確輪替（行員不固定性別）。
  - `node tools/gen-audio.mjs --dry`：28 頁、5,550 檔、尚未生成 **0** 個。
- **語音生成的一個小地雷**：首輪 245 個檔有 1 個失敗（`初回入金`，Azure 回 `terminated`），
  腳本會回報但不會中止。**重跑一次腳本即可補齊**（已完成的自動跳過，只補那 1 個）。
  日後看到「失敗 N」不要忽略，補跑到「失敗 0」為止，否則該詞會靜靜退回 Web Speech。
- **環境地雷（沿用並補充上一筆的記錄）**：headless Chrome **預設
  `prefers-color-scheme: dark`**，不先寫 `localStorage.hub-dark='0'` 就渲染，
  拿到的「淺色截圖」其實是深色版。上一筆記的 `--disable-background-networking`
  與走 CDP 兩項依舊必要。
- 待辦/已知問題：
  - 場景色 `#316745` 與星巴克的 `#3E6B4E` 同屬深綠、略近。分屬不同區塊、
    首頁上距離很遠不會並列，判定可接受；若日後覺得干擾，改用更暗的 `#1E4D3B`。
  - **`immigration.html` 的情境設定是錯的**：整頁照「持工作簽證入境」寫
    （就労ビザ／勤務先／エンジニア／在留資格認定証明書），但使用者實際是**留學生**。
    留學生入境會被問的是在留資格「留学」、学校名、在学期間、学費の支払い、経費支弁者。
    208 句的軸心都要換。已與使用者確認**另開一輪處理**，並需先決定是改寫既有頁
    還是另做一頁。`progress.md` 2026-08-07（六）記載的「使用者即將持工作簽證赴日」
    為誤記，於此更正（不修改該筆歷史紀錄）。
  - 「資格外活動許可」的申請流程（機場當場或入管窗口）對話量約 10 句，
    撐不起獨立一頁，規劃併入日後重做的入境頁。
  - 尚未 commit／push，待使用者確認。

## 2026-08-07（二）
- 類型：新增（分類）
- 影響檔案：`index.html`, `project-index.md`
- 摘要：首頁新增第四個分類**「日本在住者」**，收在住日本者會遇到的情境，
  把「語言學校面試」「入境審查」「郵局」三張卡從「旅行」移入。
  依使用者決定放在最下方（飲食 → 買い物 → 旅行 → 日本在住者）。
  卡片總數仍 27 張，三個情境頁本身**一個字都沒改**。
- 原因：這三頁是在住者視角（非觀光客），混在「旅行」裡不易找。
- 變更內容：
  - `index.html` 卡片區塊：新增一個 `.section`，三張卡依「面試 → 入境 → 在住後辦事」
    的時間動線排序；「旅行」由 12 張減為 9 張。
  - 檔尾 `SCENE` 同步調整分組註解與順序（`/* 旅行・生活 */` → `/* 旅行 */`，
    新增 `/* 日本在住者 */`）。**純註解與順序調整，不影響行為**——
    `SCENE` 以檔名為 key，與分類無關。
  - `PAGES` 陣列不分類，**未改動**。
- 驗證：
  - 靜態解析 14 項全過：4 個分類與順序、各區塊卡片組成、總數 27 且無重複、
    27 張卡與 `PAGES` 的 `file`／`title` 完全一致、`PAGES` 無孤兒、
    `SCENE` 27 筆全覆蓋且無孤兒 key、同區塊無相鄰同色、無 emoji。
  - **headless Chrome 實跑（CDP 取 live DOM）13 項全過**：渲染後 4 個分類順序正確、
    27 張卡片都拿到 `--shop` 場景色（無人退回預設藍鼠）、27 個線稿圖示 svg path 都已注入、
    新分類三張卡各自的色（`#6E5A4A` / `#3C5468` / `#A8483F`）與圖示正確、
    版面順序為旅行(1046) < 日本在住者(1592) < footer(1855)、收藏 Modal 未受影響、
    無橫向溢出、無未捕捉 JS 例外。
  - 淺色與深色各截全頁圖目視確認。
- **踩到的環境地雷（記下以免重蹈）**：`chrome --headless --dump-dom` 在本機**永遠不返回**。
  根因是 Chrome 的 GCM registration 不斷重試，背景網路請求讓 `--virtual-time-budget`
  的虛擬時間不推進。`--disable-background-networking` 是必要旗標；
  另外 `--host-resolver-rules` **不可**把外部網域 `MAP` 到本機 http server
  （https 請求打進 http server 會 hang 住連線，同樣阻塞），要用 `MAP * ~NOTFOUND`。
  最後改走 **CDP**（`--remote-debugging-port` + Node 內建 `WebSocket`）才穩定，
  可控超時、能取 live DOM 也能截圖，建議日後沿用。
- 待辦/已知問題：
  - 順帶修正 `project-index.md` 的情境頁分類表：原本按性質分（星巴克／麵包店掛在「飲食」、
    分類名寫「旅行・生活」），與 `index.html` 實際區塊不符。已改為**與 `index.html`
    的區塊及卡片順序完全一致**，並註明分類只是視覺分區、`PAGES` 與 `SCENE` 都不分類。
  - 新增分類本身不需補音檔（無新句子），`gen-audio.mjs` 不必跑。
  - 尚未 commit／push，待使用者確認。

## 2026-08-07（一）
- 類型：重構（視覺）
- 影響檔案：`app.css`（重寫）, `index.html`（樣式＋頁首結構＋場景色與圖示）, `common.js`（回首頁鈕）,
  27 個情境頁（僅 `app.css` 引用行加版本號）, `CLAUDE.md`, `project-index.md`
- 摘要：全站視覺改為**「暖簾（Noren）」風格**。核心是讓 27 個情境在視覺上真的有差別，
  並補上原本幾乎不存在的動態回饋。使用者從三個方向（紙と罫／暖簾／夜）中選定暖簾。
- 原因：舊版介面全平面（處處 1px 灰邊）、幾乎無動態、色彩只出現在 active 狀態，
  且 27 頁除色相外完全同型 —— 最大的資產（27 個真實場景）在視覺上等於零。
  使用者另要求「去除 AI 感」。
- **刻意不採用 ui-ux-pro-max 的首選建議**：該 skill 對 Language Learning App 推薦
  Claymorphism + 靛紫 `#4F46E5` + Baloo 2 圓體，這正是 AI 生成 UI 的典型指紋。
  改以日本實體設計語彙取材：日本傳統色、明朝／黑體對比、暖簾造型、漫畫分鏡氣泡。
  全案禁用紫藍漸層、玻璃擬態、大圓角配柔影、置中英雄區、emoji 圖示、Tailwind 預設色階。
- 設計要點：
  - **暖簾色帶**：頂欄／側欄標頭／收藏視窗標頭底緣以 mask 做出裂口垂片，是全站識別造型。
  - **商店街首頁**：27 張卡片各有專屬日本傳統色與線稿圖示，拼起來是一條商店街而非灰色清單。
  - **對話分鏡**：店員＝實色填滿方角氣泡＋左尖角；客人＝描邊圓角氣泡＋右尖角。
    **形狀不同，不只顏色不同**。說話者標牌改為**縱書**，保留完整「店員／客人」稱呼，資訊零損失。
  - **動態**：進場 stagger（34ms 遞延）、按壓 scale 回饋、答對脈衝／答錯 shake、
    收藏星號 pop、測驗進度條。全部只用 transform / opacity，並包 `prefers-reduced-motion`。
  - 熱力圖由 GitHub 綠改為鶯色階（原色是最容易露餡的「借來的顏色」）。
- 關鍵技術決定：
  - **衍生色一律用 `color-mix(in srgb, var(--primary) …)`**，因此
    **27 個情境頁的配色區塊一個字都沒改**（實測每頁 diff 僅 `-1/+1`，即版本號那行）。
    使用者確認 iOS 26，`color-mix()` 無相容性問題。
  - **情境頁改造全部在 `app.css` 內完成，未動 `app.js` 的 DOM**。
    氣泡尾巴、暖簾裂口、進場動畫、按壓回饋皆為純 CSS，把鐵則 1 的風險降到最低。
  - `Noto Serif JP` 早已在 27 頁載入，明朝體可直接用，不必新增字體請求；
    `index.html` 原本只載黑體，本次補上。
  - `app.css` 首次帶版本號 `?v=2`（27 頁），避免 iOS PWA 吃到舊樣式。
- 驗證：
  - **53 項自動化功能檢查全過**（headless Chrome 實跑 `hotel.html` 與 `index.html`）：
    三個主分頁切換、速查表翻牌與星號、易混淆、單字卡前後張與隨機、只練收藏、
    測驗四選項與對錯標記、常用句場景／情境／說話者篩選、全部播放、
    每句發音鈕與收藏鈕數量等於句數、furigana 標記、中文開關、
    27 張卡片皆有場景色與圖示、側欄六區塊、收藏視窗開關與三分頁。
  - 淺色與深色各渲染情境頁三視圖與首頁三狀態，逐一目視比對。
  - 深色模式抓到並修正一個真實對比度問題：`.scene-select-btn` 原本直接用 `var(--primary)`
    當字色，在深底上幾乎不可見，已改為提亮版。
  - `git diff` 確認 27 頁僅版本號一行變動，配色區塊與四個資料區塊未被觸及。
  - 改動檔案無 emoji。
- **`common.js` 的回首頁鈕同步改版**（使用者追加同意）：
  原本是 inline cssText 寫死的圓形＋毛玻璃＋金色圖示，與暖簾風格衝突，
  且 inline 樣式無法由 `app.css` 覆寫。改為**注入 `<style>` 規則 + `.home-noren` class**，
  才能寫 `::after`（裂口）與 `html[data-theme="dark"]` 覆寫。
  造型改為 44x46 方角實色小招牌，底緣有裂口，配色取自該頁 `--primary` / `--on-accent`，
  深色以 `color-mix()` 推導；移除 `backdrop-filter`。
  驗證：從 `common.js` 原始碼抽出樣式注入實際頁面實測，
  淺色 `bg=#1F3A5F` `color=#F4F5F7`、深色 `bg≈#242A35` 亮字，
  radius 2px、44x46、`backdrop-filter: none`，皆符合預期。
  另跑 8 項回歸（收藏 API、星號標記與取消、對話渲染）確認 `JPHub` 未受影響。
- **順帶修正一處文件錯誤**：`project-index.md` 原記載 common.js「index.html 與情境頁都載入」，
  實測 `index.html` **完全沒有引用 common.js**（hub 自帶一份 inline Firebase 同步邏輯）。已更正。
- 部署：commit `93a276a`（33 檔）→ push `main` → Actions run **#40 `pages build and deployment` success**。
  線上實測（`https://jp-study-list.github.io/JP-situations`）：
  - `index.html` / `hotel.html` / `immigration.html` / `common.js` 皆 200
  - `app.css?v=2` 200 且內容確認為暖簾版（非快取舊檔）
  - `audio/nanami/001336b6763098d8.mp3` 200 —— 音檔仍在，未重演 2026-08-07（日）的 `.nojekyll` 事故
  - 以 headless Chrome 直接渲染**線上**首頁與情境頁，與本機一致
- 待辦/已知問題：
  - `--r-sm` / `--r-md` / `--r-lg` / `--r-pill` 已不被 `app.css` 使用（改用 `--r-sheet: 2px`），
    27 頁中保留不刪。已更新 `CLAUDE.md` §8.4 說明，避免下次照舊規範誤判。
  - 新增情境頁時，除了 §8.6 既有步驟，還要在 `index.html` 檔尾的 `SCENE` 補一筆
    場景色與圖示；漏了不會報錯，該卡片只會退回預設藍鼠、與其他卡片同色。

## 2026-08-07（日）
- 類型：修正
- 影響檔案：`.nojekyll`（新增）, `CLAUDE.md`, `project-index.md`
- 摘要：加入根目錄 `.nojekyll`，修復 GitHub Pages 連續兩次建置逾時。
- 原因：自 `8045c54` 加入 5,042 個預生成音檔後，Pages 的 build job 連續兩次跑滿
  **15 分鐘硬性逾時**被取消（run #36 / #37），線上因此停留在 `a918bfa` 附近的版本。
  根因是根目錄缺少 `.nojekyll`，Pages 會對 5,305 個 mp3 跑完整 Jekyll 逐檔處理。
- **這個失敗最難察覺的地方**：本機完全正常、`git push` 也成功，
  只有 GitHub Actions 頁面看得出來。實際線上影響是
  `immigration.html` 404、**整個 `audio/` 目錄從未上線**，
  等於前一天做的整套 Azure 語音線上一個都沒生效，全站都在跑 Web Speech 備援。
- 診斷依據：
  - #33 / #34（`bb32c23`，音檔加入前）build 皆為 40 多秒成功；
    #36 / #37 皆為 15:01 被 cancel，分水嶺正是加入音檔的那次 commit
  - 實測線上 `audio/nanami/001336b6763098d8.mp3` 與 `immigration.html` 均為 404，
    而 `index.html` / `manifest.json` / `school-interview.html` 為 200
- 驗證（修復後）：
  - run #38 build **38 秒成功**（對比先前 15 分鐘逾時）
  - 六個音色目錄各抽一檔、七個核心檔案（三個引擎檔 + 三頁 + manifest）線上全數 200
  - **端對端**：用 `app.js` 的 `audioHash` 與 `scenarioVoices` 算出 `immigration.html`
    全部 274 個播放 URL，均勻抽 15 個查線上 —— 全數 200，線上不會退回 Web Speech
  - `node tools/gen-audio.mjs --dry`：27 頁、5,305 檔、尚未生成 0 個
- 待辦/已知問題：
  - `.nojekyll` 是空檔，容易在整理檔案時被誤刪。已在 `CLAUDE.md` §8.10 與 §8.13 標註不可刪。
  - 已在 §8.8 驗證清單補上「push 後確認線上真的更新」一項，
    避免日後再次發生「本機驗證全過但線上沒更新」。

## 2026-08-07（六）
- 類型：新增
- 影響檔案：`immigration.html`（新增）, `index.html`, `audio/`（新增 263 個 mp3）,
  `CLAUDE.md`, `project-index.md`
- 摘要：新增日本入境審查情境頁（`pageKey: immigration`，主色板岩藍 `#3C5468`），
  主題為**持工作簽證入境**。收錄 4 場景 x 4 情境 x 13 句 = 208 句、50 個單字、
  6 組易混詞、335 筆假名字典。於 `index.html` 旅行區塊補卡片與 `PAGES` 項目
  （全站情境頁 26 -> 27）。
- 原因：使用者即將持工作簽證赴日，希望出發前先熟悉入境現場可能遇到的官方問答。
- 內容設計：
  - 四場景涵蓋下飛機到出關的完整動線：入国審査（基本問答／勤務先／在留期間與住所／
    指紋與臉部拍攝）-> 在留カード（當場交付／認定証明書確認／住址登記說明／
    工作內容與資格確認）-> 税関（電子申報／免稅範圍／食品與藥品／現金申報）->
    追加確認（書類不足／別室詢問／行李開箱／放行）。
  - 依使用者選擇，三個場景走順利流程、第四場景練被追問的狀況。
  - 個人背景一律用通用佔位（株式会社さくら／エンジニア／
    技術・人文知識・国際業務／新宿区／在留一年），方便日後單點替換成本人資料。
- **刻意偏離規範**：§8.5 規定 staff 用服務業敬語，但審查官是公務員不是店家。
  本頁 `speakerLabels` 為 `{ staff: "審查官", customer: "旅客" }`，
  staff 改用公務窗口口吻（〜をお願いします／〜を教えてください／
  確認させていただきます／こちらへどうぞ／ご協力ありがとうございました），
  **不使用** いらっしゃいませ／かしこまりました／くださいませ。
  已在 `project-index.md` 與 `school-interview.html` 併列加註，避免日後被誤「修正」。
- 驗證：
  - `node --check` 通過；四資料區塊齊全、無殘留引擎碼、無 emoji、script 載入順序正確
  - 配色 14 必要變數齊全，深色基底 7 色與圓角 4 值與規範完全相符；
    主色偏暗，深色模式提亮為 `#8FAAC4` 並覆寫 `--accent-soft` / `--on-accent`
  - `pageKey` 與其餘 26 頁無衝突；`defaultCat` / `defaultScene` 均存在
  - **反向重建驗證**：用字典把每句 `jp` 還原成假名再與 `kana` 逐字比對，208 句全等。
    抓到一筆漏網（`ローマ字` 未收錄，重建後殘留漢字）
  - **furigana 端對端**：直接抄 `app.js` 的 `buildFuriganaHTML` 與排序邏輯跑一遍，
    產生 514 個 furi span，未被包住的漢字 0 句
  - `index.html` 卡片與 `PAGES` 交叉比對：27 對檔名／title 完全一致，無孤兒卡片
  - headless Chrome 實跑（本機 http server）：頁面正常渲染，四場景與單字分類進入 DOM，
    DOM 結構與 `hotel.html` 同型，無 console 錯誤
  - 語音生成：263 個成功、0 失敗；`--dry` 顯示「尚未生成 0 個」
  - **端對端音檔命中**：用 `app.js` 自身的 `audioHash` 與 `scenarioVoices` 算出本頁
    全部 276 個播放 URL，逐一確認檔案存在 —— 命中率 100%，不會退回 Web Speech
- 待辦/已知問題：
  - `app.js` 註解仍寫「音檔已由 tools/gen-audio.mjs 全量生成（5,042 個）」，
    實際已是 5,305 個。因屬鐵則 1 的檔案未擅自更動，僅為過時註解，不影響行為。
  - 本頁「行って」固定讀作 いって（字典無 おこなって）。若日後新增句子需要
    「行う」系讀音，須改用「行います／行いました」形式，否則字典無法區分。
  - `audio/` 增至 5,305 個檔、約 134MB。

## 2026-08-06（五）
- 類型：重構 / 新增
- 影響檔案：`app.js`, `tools/gen-audio.mjs`（新增）, `audio/`（新增 5,042 個 mp3）,
  `voice-check.html`（新增）, `.gitignore`（新增）, `.env.example`（新增）,
  26 個情境頁（僅改 script 引用）, `project-index.md`
- 摘要：語音層從瀏覽器 Web Speech 改為 **Azure Neural TTS 預生成音檔**，
  並從單一「店員女聲／客人男聲」擴充為 **6 音色、每個情境輪替配對**。
  - 新增 `tools/gen-audio.mjs`：以 `node:vm` 求值各情境頁的第一個 script 取出資料，
    呼叫 Azure REST TTS 批次生成，輸出 `audio/<音色>/<hash>.mp3`。
    支援 `--dry`（只統計）、`--sample`（試聽）、`--tier=s0`（付費層提速）、斷點續傳、孤兒回報。
  - `app.js` 播放層改為 audio-first，Web Speech 降為 fallback（音檔 404 或載入失敗時接手）。
  - 順帶修正 Web Speech 本身：**移除 pitch 位移**（原本 staff 1.15／customer 0.85，
    是「聽起來像機器人」的主因）、音色改用品質評分排序、性別判斷改用真實語音名稱對照表
    （原本的 `/female|女|f\b/` regex 命中率為零，實際是隨機取 `voices[0]`／`[1]`）。
- 原因：使用者反映全站對話語音機械化，且整站只有兩個聲音。
- 關鍵設計決定（皆有實測依據，勿隨意改動）：
  - **檔名用內容 hash**（`audio/<音色>/<16位hex>.mp3`）：同一句話跨情境頁自動共用，
    新增情境頁時已存在的句子不必重新生成。實測 5,741 個引用去重成 5,042 個檔。
  - **hash 用同步的 FNV-1a 變體，不用 `crypto.subtle`**：後者是非同步的，而 iOS Safari 要求
    `audio.play()` 必須在使用者手勢的同步呼叫堆疊內，`await` 之後會失去手勢授權而靜音。
  - **全站共用同一個 `<audio>` 元素**：iOS 只信任被手勢解鎖過的那一個，換句子只換 `src`。
  - **播放世代編號 `audioToken`**：切歌造成的 `AbortError` 不可被誤判成「檔案不存在」。
  - **`AUDIO_ENABLED` 開關**：音檔尚未生成時設 false，否則每次播放要先吃一個 404 才 fallback。
  - `scenarioVoices(pageKey, sceneKey, scenarioIndex)` 在 `app.js` 與 `gen-audio.mjs`
    **各有一份完全相同的實作**，任一邊改動都必須同步，否則組出的檔名對不上。
- 驗證：
  - `node --check app.js` 通過
  - hash 一致性：Node 與瀏覽器兩份實作比對 288 筆全等；全站 4,865 句零碰撞
  - `scenarioVoices` 一致性：240 組情境兩邊完全相符，涵蓋全部 18 種配對組合
  - 生成：5,000 成功、0 失敗；產出 5,042 檔，無零位元組或異常小檔
  - **端對端驗證**（最關鍵）：用 `app.js` 自己的程式碼算出 26 頁全部 5,741 個播放 URL，
    逐一確認檔案存在 —— 全數命中，沒有任何一句會退回 Web Speech
  - 使用者實機確認：單句播放、全部播放、跨情境換人聲、單字固定音色，皆正常
- 待辦/已知問題：
  - `audio/` 佔 **116MB**（5,042 檔，平均 23.5KB，24kHz/48kbps 單聲道 mp3）。
    GitHub Pages 站台上限 1GB，無虞，但 clone 會變慢。
  - **新增情境頁後必須補跑 `node tools/gen-audio.mjs`**，否則該頁會退回 Web Speech。
  - 音色池刻意排除 `aoi`（葵），使用者試聽後不喜歡；設定仍保留在 `VOICE_IDS` 以便改回。
  - HD 音色 `ja-JP-Sakura` / `ja-JP-Haruto`（MAI-Voice-2-Flash）在 F0 免費層回 502，
    需付費層才能用。
  - `voice-check.html` 是診斷用工具頁，非情境頁，不在 `index.html` 掛入口，可隨時刪除。

## 2026-08-06（四）
- 類型：新增
- 影響檔案：school-interview.html（新增）, index.html, project-index.md
- 摘要：新增語言學校面試情境頁（`pageKey: school-interview`，主色靛藍 `#3F3A70`），
  收錄 4 場景 x 4 情境 x 14 句 = 224 句面試問答、56 個單字、6 組敬語易混、518 筆假名字典。
  於 `index.html` 旅行區塊補卡片與 `PAGES` 項目（全站情境頁 25 -> 26）。
- 原因：使用者即將參加線上語言學校面試，需要針對真實背景演練可能的提問。
- **刻意偏離規範**：§8.5 規定 staff 用服務業敬語，但面試情境完全不適用。
  本頁 `speakerLabels` 改為 `{ staff: "老師", customer: "學生" }`，
  老師端用尊敬語提問（〜されていますか／ご〜／教えてください），
  學生端用謙讓語應答（〜と申します／〜ております／〜と考えております）。
  已在 `project-index.md` 情境頁表格加註，避免日後被誤「修正」回店家用語。
- 驗證：
  - `node --check` 通過；四資料區塊齊全、無殘留引擎碼（sort／buildFurigana／三個狀態宣告）
  - 配色 14 必要變數齊全，深色基底 7 色與圓角 4 值與規範完全相符
  - `pageKey` 與其餘 25 頁無衝突；`defaultCat: interview`／`defaultScene: intro` 均存在
  - KANJI_READINGS：以最長匹配模擬，224 句漢字零缺漏
  - **反向重建驗證**（新做法）：用字典把每句 `jp` 還原成假名再與 `kana` 欄位逐字比對，
    224 句全等。此法抓到覆蓋率檢查抓不到的錯誤（`母は` 應讀 ははは，原本漏一個 は）
  - `index.html` 卡片與 `PAGES` 交叉比對：26 對檔名／title 完全一致，無孤兒卡片
  - headless Chrome 實跑：深淺兩色模式渲染正常、四場景與對話內容進入 DOM、無 console 錯誤
- 待辦/已知問題：
  - 內容含三處佔位資料，使用者需自行替換為本人資訊：姓名（林）、出身地（台北）、
    面試官姓氏（佐藤）。其餘背景（材料資源工學、補習班數學講師、鎌倉親戚、
    J-POP 動機、經費自付＋父母資助）皆為使用者實際情況。
  - 「行って」在本頁固定讀作 おこなって；若日後新增句子需要 いって，
    請改用「行きます／行きました」形式，否則字典無法區分。

## 2026-08-06（三）
- 類型：修正
- 影響檔案：manifest.json（新增）, CLAUDE.md, project-index.md
- 摘要：補回缺失的 `manifest.json`。`start_url` / `scope` / `icons.src` 全用
  `/JP-situations/...` 絕對路徑，`display: standalone`，theme_color 對齊
  `index.html` 的 `<meta name="theme-color">`（`#1f3a5f`）。
- 原因：`index.html:7` 引用了此檔但它從未進入 repo，線上 404，PWA metadata 失效。
- 驗證：JSON 可解析；icons 宣告尺寸與實際 PNG 尺寸相符（192x192 / 512x512）。

## 2026-08-06（二）
- 類型：新增
- 影響檔案：electronics-store.html, index.html
- 摘要：新增家電量販店情境頁（`pageKey: electronics-store`，shape 分生活／廚房／影音家電），
  並在 `index.html` 買い物區塊補卡片與 `PAGES` 項目。
- 原因：補齊買い物類情境。
- 驗證：`node --check` 通過；四個資料區塊齊全、無殘留引擎碼；
  `defaultCat: appliance` 與 `defaultScene: consult` 皆存在；卡片 title 與 PAGES title 一致。

## 2026-08-06（一）
- 類型：重構 / 文件
- 影響檔案：CLAUDE.md, project-index.md（新增）, progress.md（新增）, CLAUDE-old.md（刪除）
- 摘要：執行專案初始化自舉。將 `CLAUDE-old.md` 的全部專案規範（檔案結構、鐵則、
  PAGE_CONFIG／配色／資料內容規格、新增情境流程、遷移流程、驗證清單、已知注意事項）
  整合進 `CLAUDE.md` §8，並補齊 Firebase、部署、資料源、自動化欄位；
  建立 `project-index.md` 與本檔。
- 原因：統一為單一規範檔，避免兩份文件並存分歧。
- 待辦/已知問題：
  - 舊規範「本機不使用 git、不要執行 git 指令」已作廢（repo 已有 git 與 GitHub remote），
    改依 `CLAUDE.md` §4 ABCD 規則。

---

<!-- 以下為初始化前的既有歷史，由 git log 摘錄，僅供對照 -->

## 更早（git log 摘錄）
- `e38bb7b` 加入電車（`train-subway.html`）
- `5fa2a9f` 修正四頁的 pageKey 與檔名一致
- `4c9d28a` Add files via upload
- `b31d72d` Update taxi.html
- `3081487` Update sushi.html
